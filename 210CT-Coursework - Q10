
def subSeqGenerator(L):
    sub_seq = []
    sub_seq_count = 1
    for i in range(len(List)-1):
        if List[i]>List[i+1]:
            sub_seq.append(sub_seq_count)
            sub_seq_count = 1
        else:
            sub_seq_count +=1
    sub_seq.append(sub_seq_count)   
    return sub_seq


def IndexSearch(List):
    total_to_index = 0
    value = max(subSeqGenerator(List))
    up_to_index = List.index(max(value))
    print(up_to_index)
    for i in List:
        if i == value:
            return total_index
        else:
            total_index = total_index+i

    return total_index
    
print(subSeqGenerator(List))
print(linear_search(List))

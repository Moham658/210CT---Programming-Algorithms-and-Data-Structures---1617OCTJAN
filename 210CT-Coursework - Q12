def in_order(tree):
    
  empty_stack = []
  rules = bool(empty_stack != None or tree != None)
  while rules == True:
      if tree != None:
          empty_stack.append(tree)
          tree = tree.left
      elif empty_stack != None:
          if len(empty_stack) == 0:
              break
          tree = empty_stack.pop()
          print(tree.value)
          tree = tree.right

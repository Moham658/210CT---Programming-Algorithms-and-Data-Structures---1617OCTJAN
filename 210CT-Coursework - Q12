def in_order(tree):
  empty_stack = []
  while empty_stack != None or tree != None:
    if tree != None:
      empty_stack.append(tree)
      tree = tree.left
      
    elif empty_stack != None:
        tree = empty_stack.pop()
        print(tree.value)
        tree = tree.right

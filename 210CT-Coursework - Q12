def in_order(tree):
    
  stack = []
  
  while stack != None:
      if tree != None:
          stack.append(tree)
          tree = tree.left
      elif stack != None:
          if len(stack) == 0:
              break
          tree = stack.pop()
          print(tree.value)
          tree = tree.right

def factorial(n):
    lower = 1
    while n >= 1:
        lower = lower * n
        n = n - 1
    return(int(lower))


def trailing_zeroes(n):
    check = factorial(n)
    zeroes = 0
    while check % 10 == 0:
        zeroes += 1
        check = check/10
    return(zeroes)
